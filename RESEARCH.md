# Local research

`particle-morph-research/` is a local reference only. It is gitignored and is not part of Scree.

Do not copy those trees into `src/`. The engine is a rewrite of the morph ideas: fixed particle budget, equal-length image targets, one field, progress-driven cloud.
